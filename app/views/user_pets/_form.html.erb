<%= form_with(model: user_pet) do |form| %>
  <% if user_pet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_pet.errors.count, "error") %> prohibited this user_pet from being saved:</h2>

      <ul>
        <% user_pet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :pet_id %>
    <%= form.text_field :pet_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
